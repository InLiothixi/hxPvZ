package todlib.todparticle;

enum abstract ParticleTracks(Int) from Int to Int 
{
    var RED;
	var GREEN;
	var BLUE;
	var ALPHA;
	var BRIGHTNESS;
	var SPIN_SPEED;
	var SPIN_ANGLE;
	var SCALE;
	var STRETCH;
	var COLLISION_REFLECT;
	var COLLISION_SPIN;
	var CLIP_TOP;
	var CLIP_BOTTOM;
	var CLIP_LEFT;
	var CLIP_RIGHT;
	var ANIMATION_RATE;
	var NUM_PARTICLE_TRACKS;
}
